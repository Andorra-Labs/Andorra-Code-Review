# Andorra OCR ensemble review on pull requests.
#
# === SECURITY MODEL ===
# This workflow is `pull_request_target`-triggered so it has secret access
# even on fork PRs. The safety story:
#
#   1. The OCR binary is `go install`-ed from a PINNED UPSTREAM VERSION.
#      It is never built from PR source. Update the version below when you
#      want to adopt a new release.
#   2. The config file (.ocr/config.json) is read from the BASE ref. Fork
#      PRs cannot redirect API calls or change scanner config by editing
#      that file in their branch.
#   3. PR head code is checked out into pr-head/ as SANDBOXED DATA. The
#      OCR binary reads those files as text (it's a static review tool —
#      it never executes the code under review). Secrets are passed only
#      to the trusted OCR binary, not to anything sourced from PR head.
#
# This is the standard mitigation for the pull_request_target pwn-request
# antipattern: https://securitylab.github.com/research/github-actions-preventing-pwn-requests/
#
# === SETUP ===
#   1. Locally:   ocr config ui     (configure scanners + arbiter)
#   2. Locally:   ocr config export (writes .ocr/config.json with placeholders)
#   3. Commit:    git add .ocr/config.json && git commit
#   4. In CI:     add OCR_ANTHROPIC_API_KEY / OCR_OPENAI_API_KEY /
#                 OCR_BEDROCK_API_KEY / OCR_BEDROCK_REGION (whichever your
#                 .ocr/config.json placeholders reference) as repo or org
#                 secrets.
name: Andorra OCR Review

on:
  pull_request_target:
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: read
  pull-requests: write
  actions: read

jobs:
  review:
    runs-on: ubuntu-latest
    if: ${{ !github.event.pull_request.draft }}
    steps:
      - name: Check out BASE ref (trusted — controls .ocr/config.json)
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.base.sha }}
          fetch-depth: 0

      - name: Detect committed config
        id: detect
        run: |
          if [ -f .ocr/config.json ]; then
            echo "present=true" >> "$GITHUB_OUTPUT"
          else
            echo "::notice::.ocr/config.json not present in base ref — skipping Andorra OCR review."
            echo "present=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Fetch PR head into pr-head/ via raw git (avoids actions/checkout
              refusing fork-head SHAs under pull_request_target; see
              https://github.blog/changelog/2026-06-18-safer-pull_request_target-defaults-for-github-actions-checkout/)
        if: steps.detect.outputs.present == 'true'
        env:
          # head.repo.clone_url is set even for forks. We treat the result as
          # data: the trusted OCR binary reads files but never executes them.
          HEAD_REPO_URL: ${{ github.event.pull_request.head.repo.clone_url }}
          BASE_REPO_URL: ${{ github.event.pull_request.base.repo.clone_url }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          BASE_REF: ${{ github.event.pull_request.base.ref }}
          # GITHUB_TOKEN authenticates the raw clone for PRs from PRIVATE forks.
          # Public forks fetch fine without auth. The token is GitHub-issued and
          # only read by git's local config inside pr-head/, never echoed.
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git init pr-head
          cd pr-head
          git config advice.detachedHead false
          # Attach token to both remotes via http.extraheader so fetches from
          # private fork repos succeed. Restrict the header to github.com hosts
          # so we never send the token to a non-GitHub URL.
          AUTH_HDR="AUTHORIZATION: bearer ${GH_TOKEN}"
          git config "http.https://github.com/.extraheader" "${AUTH_HDR}"

          git remote add origin "${HEAD_REPO_URL}"
          git remote add base "${BASE_REPO_URL}"

          # Full fetch (no --depth) so `git merge-base origin/<base>..HEAD`
          # always succeeds — shallow clones miss the merge base when the PR
          # diverged more than N commits ago. Use --filter=blob:none to
          # skip blob downloads and keep this fast on large repos.
          git fetch --filter=blob:none --no-tags base "${BASE_REF}"
          git fetch --filter=blob:none --no-tags origin "${HEAD_SHA}"

          git checkout --detach "${HEAD_SHA}"
          # Make origin/<base> resolvable for `--from`.
          git update-ref "refs/remotes/origin/${BASE_REF}" "$(git rev-parse base/${BASE_REF})"

      - name: Set up Go
        if: steps.detect.outputs.present == 'true'
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Install Andorra OCR from a pinned fork release
        if: steps.detect.outputs.present == 'true'
        env:
          # SECURITY: this must point at a tag/commit YOU trust in the
          # ANDORRA FORK repo (which is where the ensemble CLI lives).
          # Upstream `open-code-review` does not have `review --ensemble` —
          # do not point ANDORRA_OCR_REF at an upstream tag.
          # Never use a branch, @HEAD, or a ref the PR author can influence.
          ANDORRA_OCR_REPO: https://github.com/Andorra-Labs/Andorra-Code-Review.git
          ANDORRA_OCR_REF: main # ← replace with a fork release tag, e.g. v0.1.0
        run: |
          # Build from a clean clone of the fork at the pinned ref. We don't
          # `go install github.com/.../cmd/opencodereview@TAG` because the
          # fork keeps the upstream module path, so that command would fetch
          # upstream code, not the fork.
          tmp="$(mktemp -d)"
          git -c advice.detachedHead=false clone --depth 1 \
            --branch "${ANDORRA_OCR_REF}" \
            "${ANDORRA_OCR_REPO}" "${tmp}/src"
          (cd "${tmp}/src" && go build -o "$(go env GOPATH)/bin/opencodereview" ./cmd/opencodereview)
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"

      - name: Run ensemble review (trusted binary + base config + sandboxed PR data)
        if: steps.detect.outputs.present == 'true'
        working-directory: pr-head
        env:
          # Names match what `ocr config export --placeholder-secrets` wrote
          # into .ocr/config.json. Edit for your providers.
          OCR_ANTHROPIC_API_KEY: ${{ secrets.OCR_ANTHROPIC_API_KEY }}
          OCR_OPENAI_API_KEY:    ${{ secrets.OCR_OPENAI_API_KEY }}
          OCR_BEDROCK_API_KEY:   ${{ secrets.OCR_BEDROCK_API_KEY }}
          OCR_BEDROCK_REGION:    ${{ secrets.OCR_BEDROCK_REGION }}
        run: |
          opencodereview review \
            --ensemble \
            --config ../.ocr/config.json \
            --from origin/${{ github.event.pull_request.base.ref }} \
            --to ${{ github.event.pull_request.head.sha }} \
            --format json \
            > ../review.json

      - name: Post review comments
        if: steps.detect.outputs.present == 'true' && hashFiles('review.json') != ''
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');
            const data = JSON.parse(fs.readFileSync('review.json', 'utf8'));
            const comments = (data.comments || []).map(c => ({
              path: c.path,
              line: c.end_line || c.start_line,
              side: 'RIGHT',
              body: c.content + (c.suggestion_code
                ? '\n\n```suggestion\n' + c.suggestion_code + '\n```'
                : '')
            }));
            const summary = data.ensemble
              ? `**Andorra OCR ensemble:** ${data.ensemble.scanners.length} scanners, ${data.ensemble.groups.length} groups → ${comments.length} accepted bug(s).`
              : `**Andorra OCR:** ${comments.length} comment(s).`;
            if (comments.length === 0) {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: summary + '\n\n_No accepted-bug findings on this diff._'
              });
              return;
            }
            try {
              await github.rest.pulls.createReview({
                owner: context.repo.owner,
                repo: context.repo.repo,
                pull_number: context.issue.number,
                commit_id: '${{ github.event.pull_request.head.sha }}',
                event: 'COMMENT',
                body: summary,
                comments
              });
            } catch (err) {
              core.warning(`createReview failed: ${err.message}. Falling back to issue comment.`);
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: summary + '\n\n' + comments.map(c =>
                  `**${c.path}:${c.line}** — ${c.body}`).join('\n\n---\n\n')
              });
            }
